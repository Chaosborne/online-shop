export interface IProduct {
  id: string;
  itemBrand: string;
  itemCategory: string;
  itemDescription: string;
  itemImg: string;
  itemName: string;
  itemPrice: number;
  itemQuantity: number;
  itemTotalPrice: number;
}
