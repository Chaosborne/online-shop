export interface IProduct {
  id: string;
  images: string[];
  itemBrand: string;
  itemCategory: string;
  itemDescription: string;
  itemName: string;
  itemPrice: number;
  itemQuantity: number;
  itemTotalPrice: number;
}
